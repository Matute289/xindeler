//! Xindeler dedicated server — Bevy headless shell (MinimalPlugins) that will
//! embed the authoritative Veloren sim (EM-4.1). Skeleton (EM-0.7).

use bevy::app::{App, AppExit};

fn main() -> AppExit { App::new().run() }
